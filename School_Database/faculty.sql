CREATE TABLE faculty (
    faculty_id INTEGER PRIMARY KEY AUTOINCREMENT,
    faculty_name TEXT,
    faculty_position TEXT,
    faculty_department TEXT,
    faculty_email TEXT,
    faculty_phone TEXT
);
